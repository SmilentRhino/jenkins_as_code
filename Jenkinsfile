pipeline {
    agent master
    stages {
        stage('Init') {
            steps {
                echo 'Go go go'
                sh 'pwd'
                sh 'ls'
                sh 'cat Jenkinsfile'
            }        
        }
    }
}
